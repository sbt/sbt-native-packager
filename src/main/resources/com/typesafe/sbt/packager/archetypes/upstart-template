# generated upstart config

description "${{descr}}"
author "${{author}}"

# Stanzas
#
# Stanzas control when and how a process is started and stopped
# See a list of stanzas here: http://upstart.ubuntu.com/wiki/Stanzas#respawn

# When to start the service
start on runlevel [2345]

# When to stop the service
stop on runlevel [016]

# Automatically restart process if crashed. Tries ${{retries}} times every ${{retryTimeout}} seconds
respawn
respawn limit ${{retries}} ${{retryTimeout}}


pre-start script
	# set the working directory of the job processes
    chdir ${{chdir}}
end script

# Start the process
script
  exec start-stop-daemon --chuid ${{daemon_user}} --exec ${{chdir}}/bin/${{exec}} --start
end script